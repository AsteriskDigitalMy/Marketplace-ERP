# 3.6.2 Integration Architecture and Specifications — Frontend Requirements

**Source:** Section 3.6.2

---

## Screens

### Integration overview (`/sap/config`)

**Architecture diagram** (static): Middleware + Interface Services between Operational Systems ↔ SAP. No direct DB access — informational.

---

### Interface configuration (`/sap/config/interfaces`)

| Interface | Protocol | Endpoint (mock) | Status |
|-----------|----------|-----------------|--------|
| Material sync | OData | `...` | Online |
| P2P voucher | BAPI | `...` | Online |
| O2C voucher | BAPI | `...` | Online |
| CO settlement | RFC | `...` | Offline |

Protocol badges: REST, OData, RFC/BAPI.

Test connection button per interface (mock success/fail).

---

### Sync mode settings (`/sap/config/sync-modes`)

| Mode | Config |
|------|--------|
| Real-time | Document types multi-select (receipts, deliveries, invoices) |
| Scheduled | Cron picker — daily/weekly for non-core data |
| Manual trigger | Enabled roles |

Per document type matrix: which mode applies.

---

### Security settings (read-only + admin edit)

| Setting | Display |
|---------|---------|
| Transport | HTTPS enforced |
| Sensitive data | ShangMi encryption indicator |
| Auth | Account + Token |
| Audit logging | Enabled |

Token rotate action (mock) with confirm dialog.

---

### Exception policy (`/sap/config/exceptions`)

| Policy | Value |
|--------|-------|
| Auto-retry count | 3 (editable) |
| Retry interval | seconds |
| Alert recipients | IT + Finance multi-select |
| Consistency check schedule | daily time picker |

---

## Mock Data Shape

```ts
interface IntegrationConfigDto {
  Interfaces: { Name: string; Protocol: 'rest' | 'odata' | 'rfc'; Endpoint: string; IsOnline: boolean }[]
  SyncModes: { DocumentType: string; Mode: 'realtime' | 'scheduled' | 'manual' }[]
  Security: { HttpsEnforced: boolean; ShangMiEnabled: boolean; TokenExpiry: string }
  RetryPolicy: { MaxRetries: number; IntervalSeconds: number; AlertRecipients: string[] }
}
```

Mocks: `src/mocks/sap/integration-config.ts`.

---

## Acceptance Criteria

- [ ] Architecture overview and interface list with protocol/status
- [ ] Sync mode configuration per document type
- [ ] Security and retry policy settings UI
- [ ] Test connection mock per interface
- [ ] Four async states on config screens
