# 3.4.8 Production Cost Fine-Grained Management — Frontend Requirements

**Source:** Section 3.4.8  
**Purpose:** Real-time WO cost aggregation; closure accounting; variance vs standard; SAP sync.

---

## Screens

### Cost dashboard (`/mes/costs`)

**Filters:** workshop, product, date range.

KPIs: total material, labor, overhead, outsourcing, rework — trend sparklines.

---

### Work order cost (`/mes/costs/work-orders/:id`)

**Real-time aggregation panel** (mock live updates):

| Dimension | Formula hint | Amount |
|-----------|--------------|--------|
| Material | picked qty × moving avg price | |
| Labor | hours × rate / piece wages | |
| Manufacturing overhead | allocated | |
| Outsourcing | fees | |
| Rework | from rework orders | |
| **Total** | | |
| **Unit cost** | total / output | |

Progress bar during production — % of estimated final cost.

---

### Closure comparison (links 3.4.2.5)

Standard cost (PDM) vs actual — variance table + chart.

---

### Cost analysis (`/mes/costs/analysis`)

Slice by workshop, product, process, date — identify waste hotspots (heatmap table mock).

**SAP sync** status per closed WO.

---

## Mock Data Shape

```ts
interface WorkOrderCostDto {
  WorkOrderId: string
  WorkOrderNumber: string
  MaterialCost: number
  LaborCost: number
  ManufacturingOverhead: number
  OutsourcingCost: number
  ReworkCost: number
  TotalCost: number
  UnitCost: number
  StandardCost: number
  Variance: number
  VariancePercent: number
  IsRealTime: boolean
  SapSyncStatus: string
}
```

Mocks: `src/mocks/mes/costs.ts`.

---

## Acceptance Criteria

- [ ] WO cost view with real-time dimension breakdown
- [ ] Standard vs actual variance on closed WOs
- [ ] Analysis by workshop/product/process/date
- [ ] SAP sync indicator
- [ ] Four async states
